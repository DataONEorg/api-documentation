@startuml images/morpho_save_new_package_seq.png

  
   participant "abractPackage: AbstractDataPackage" as abstract_package <<Morpho>>
   participant ": FileSystemDataStore" as data_store <<Morpho>>
   participant ": MorphoOREDataPackage extends DataPackage" as d1_datapackage <<Morpho>>
   
   abstract_package -> data_store: delete(LOCAL)
   activate data_store
   abstract_package <- data_store: delete(abstractPackage)
   abstract_package -> data_store: getOREDataPackage()
   data_store <- d1_datapackage: getMetadataMap()
   data_store -> data_store: extract the identifier list from the metadata map
   loop iterate the identifier list
      data_store -> data_store: delete(identifier)
   end
   deactivate data_store
  @enduml
